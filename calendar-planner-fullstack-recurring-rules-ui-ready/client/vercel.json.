{
  "version": 2,
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://planificador-de-tareas.onrender.com/api/:path*" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
